import { StyleSheet } from "react-native";

export const cardSharedStyles = StyleSheet.create({
  emojiWrap: {
    width: 40,
    alignItems: "center",
  },
  emoji: {
    fontSize: 24,
  },
  info: {
    flex: 1,
    gap: 1,
    paddingHorizontal: 8,
  },
});
